#include <stdio.h>
#include <math.h>

void main(){

    //x raised to y
    int x=2 ,y=3 ,t ,omega;

// printf(pow(x,y));
pow(2,2);
//sin x
sin(x);
cos(x);
sin(y/sqrt(pow(x,2) + pow(y,2)));
// z= (alpha + beta)/sin(theta*3.14/180) +fabs(x);
y=sin(omega*t)*cos((omega*3.14)/t);





}



#include<stdio.h>

int main(){
    int i,age,age2;
    for(i=1;i<=10;i++){
        printf("enter youre age:");
        scanf("%d",&age);
        if(age>12){
            // continue;//if age biger then 12 then this code will not run in continune satament but loop continune--
            // break;//if age bigger then 12 the loop become turminatede--in break statement-
        }
        
        printf("deep\n");
         printf("deep\n");
          printf("deep\n");
    }
}